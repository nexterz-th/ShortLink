# ห้ามเข้าถึงโฟลเดอร์นี้ผ่านเว็บโดยตรง (เผื่อกรณีตั้ง document root ไว้ที่รากโปรเจกต์)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
